export const sumar = (a, b) => {
  return a + b;
};

export const restar = (a, b) => {
  return a - b;
};

export default function saludar() {
  return "holis";
}


/*
NODE - COMMAND JS <- nativa

module.exports = {
}


const sumar = require()

--------------------

tipo MODULE <- ES6 - FRONT

import

export o export default

*/